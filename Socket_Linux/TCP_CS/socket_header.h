#include <unistd.h>     // for close
#include <sys/socket.h> // for socket, bind, listen, accept
#include <netinet/in.h> // for sockaddr_in, AF_INET, htons, INADDR_ANY
#include <arpa/inet.h>  // for inet_ntoa

